# 作品展示改进清单

> **2026-09-07 复核更新**：以下条目经实际代码核对已完成，正文中的“待做”描述已过时：
> - P0-1 美术统一 → **已完成**：pixel / classic 双主题皮肤，主菜单可切换（皮肤化设计，避免回炉）
> - P0-2 视觉特效 → **大部分完成**：FxManager 统一封装（命中/爆炸/升级/死亡消散/拾取/弹射/闪电链/冰冻等）
> - P1-5 敌人扩充 → **完成**：召唤怪/冲锋怪/治疗怪 + 自爆/分裂/护盾（11 种敌人）
> - P1-6 Boss 多样化 → **完成**：3 种 Boss（基础红/召唤魔像/弹幕机械），血量 66%/33% 三阶段、技能逐阶解锁（环形/扇形/追踪/冲锋/地面AOE）、阶段变色/移速/伤害递增、入场演出（震屏+横幅）
> - P1-9 成就系统 → **完成**：22 成就 6 系 + 永久加成 + 解锁弹窗 + 成就场景 + 脚本化自测（24 项 PASS）
> - P2-10 游戏内设置 → **完成**：主菜单设置面板（音乐/音效音量、画质低中高、静音，存档持久化）
> - 关卡化结构 → **完成**：3 关、每关 15 波、通关后可继续征战（无尽）、跨关继承 build、关卡差异化 Boss/敌人偏移/特殊规则
> - 新被动 → **完成**：吸血/弹射/冰冻/灼烧/闪电链全部实装并接入命中系统
> - 数值工具 → 已完成：balance-report 审计、倍速调试、AI 自动游玩、怪物增强调试
> - 部署 → 已完成：ESA Pages（GitHub 远端）

> **当前真正剩余项**：P0-3 音频资源（用户暂缓，接口已预留）、P1-7 角色选择（想法已记录，时机未到）、P3-16 每日挑战/种子、P3-17 武器进化/超武、发布物料（README/SEO/截图/图标）。

> 项目：KEEP LIVING — 2D 割草生存（Phaser 3 + TypeScript + Vite）
> 整理时间：2026-08-29
> 目的：明确从"可玩原型"到"可展示作品"还需要补齐的内容，按优先级排序。

---

## 一、当前已完成的基础能力

| 模块 | 状态 | 说明 |
|---|---|---|
| 核心玩法 | ✅ | WASD/摇杆移动、自动攻击、敌人波次、升级三选一 |
| 武器系统 | ✅ 9种 | 基础枪、机枪、霰弹枪、激光、火箭筒、回旋镖、光剑、无人机、环形冲击 |
| 被动技能 | ✅ 9种 | 生命恢复、荆棘、经验加成、金币加成、吸血、弹射、冰冻、灼烧、闪电链 |
| 敌人类型 | ✅ 11种 | 普通、快速、重装、远程、精英、Boss、自爆、分裂、护盾、召唤、冲锋、治疗 |
| Boss 战 | ✅ | 每5波出现，顶部大血条，弹幕攻击 |
| 双爆系统 | ✅ | 暴击率/暴击伤害，暴击伤害数字飘字 |
| 神秘商店 | ✅ | Boss 前出现，金币购买武器/被动/道具 |
| 地形系统 | ✅ | 数据驱动障碍物，玩家/敌人/子弹碰撞，支持新区域扩展 |
| 小地图 | ✅ | 实时显示玩家/敌人/Boss/障碍物 |
| 敌人寻路 | ✅ | 前方探测 + 侧向避让，不蹭墙 |
| 存档/统计 | ✅ | 最高分、总击杀、游戏次数 |
| 调试面板 | ✅ | 升级、加金币、无敌、刷怪等 |
| 引导系统 | ✅ | 分步新手提示 |
| 移动端 | ✅ | 虚拟摇杆 |
| 音频框架 | ✅ | AudioManager 单例，支持 BGM/SFX/音量控制，但**无实际音频资源** |
| 高分屏适配 | ✅ | DPR 缩放 |

---

## 二、P0 — 展示第一印象（必须做）

### 1. 美术资源统一化
**现状**：全部使用占位素材——玩家是青色三角形、敌人是 tint 染色的圆形 + emoji 眼睛、武器图标是 emoji、障碍物是灰蓝色矩形、背景是纯黑 + 网格线。

**需要做**：
- 确定统一美术风格（推荐：像素风 / 霓虹赛博 / 扁平矢量，三选一）
- 玩家角色精灵图（含朝向动画，至少 idle + move 4方向或8方向）
- 6种敌人各自的独立精灵图（不再靠 tint 区分）
- 8种武器的弹道/特效图（激光、火箭爆炸、光剑剑气、无人机等）
- 地图背景纹理（草地/废墟/太空等，替换纯黑网格）
- 障碍物美术（岩石/墙体/水晶各自有贴图）

**为什么重要**：展示时第一眼就是画面，占位素材会让人觉得"没做完"。

### 2. 视觉特效
**现状**：几乎没有特效——命中只有伤害数字，爆炸没有粒子，升级没有闪光，死亡没有消散。

**需要做**：
- 命中粒子（火花/血溅）
- 爆炸特效（火箭筒、Boss弹幕）
- 升级闪光 + 屏幕微震动
- 敌人死亡消散动画
- 拾取物吸附光效
- 武器射击 muzzle flash

### 3. 背景音乐 + 音效
**现状**：AudioManager 框架已就绪，但没有加载任何音频资源，游戏全程静音。

**需要做**：
- 至少 1 首循环 BGM（主菜单 + 战斗可各一首）
- 核心音效：射击、命中、爆炸、升级、拾取、受伤、死亡、Boss出现、按钮点击
- 主菜单设置里加音量开关/滑块

**为什么重要**：静音的游戏展示感染力差一半，音效是"手感"的重要组成。

### 4. 主菜单与 UI 美化
**现状**：主菜单是文字按钮 + 标题，设置页面是空壳；升级三选一和商店界面是半透明圆角矩形，功能完整但视觉朴素。

**需要做**：
- 主菜单：标题 Logo 设计、背景动效、按钮 hover/点击反馈
- 设置页面：音量、画质、按键说明（目前设置按钮可能是空的）
- 升级三选一：卡片加图标、稀有度光效、描述排版优化
- 商店界面：商品卡片美化、金币显示、购买反馈
- 结算画面：数据展示美化、再来一局/主菜单按钮

---

## 三、P1 — 内容深度（展示游戏性）

### 5. 敌人种类扩充
**现状**：9种敌人（含 2026-09-01 新增的自爆/分裂/护盾），行为模式 7 种（直冲、Z字、远程保持距离、Boss弹幕、自爆、分裂、护盾正面减伤）。

**继续扩充**：
- 再补充 3-6 种敌人，每种有独特行为：
  - 召唤怪（持续召唤小怪）
  - 冲锋怪（蓄力后高速冲刺）
  - 治疗怪（给周围敌人回血）
  - 精英词缀（狂暴 / 护盾 / 分裂，2026-09-01 基础敌人已具备对应机制，可复用）
- 每种敌人有独立外观和音效

### 6. Boss 多样化
**现状**：只有1种 Boss，行为是缓慢追击 + 3秒8方向弹幕。

**需要做**：
- 至少 3-5 种 Boss，每种有独特技能组合：
  - 冲锋型 Boss（冲撞 + 震地）
  - 射手型 Boss（追踪弹 + 弹幕网）
  - 召唤型 Boss（持续召唤精英怪）
- Boss 进入时的演出（屏幕震动 + 提示 + BGM 切换）
- Boss 多阶段（血量低于50%进入二阶段，技能变化）

### 7. 角色选择
**现状**：只有1个角色，固定初始属性和武器。

**需要做**：
- 3-5 个可选角色，每个有：
  - 不同初始属性（血量/速度/幸运）
  - 不同初始武器
  - 独特被动（如"暴击率+10%"、"拾取范围+50%"）
- 角色选择界面（立绘 + 属性对比 + 解锁条件）

### 8. 多地图/区域
**现状**：只有1张 3000x3000 地图，16个固定障碍物。

**需要做**：
- 利用已有的数据驱动地形系统，做 2-3 张主题地图：
  - 废墟城市（墙体障碍多）
  - 开阔草原（障碍物少，敌人刷新快）
  - 洞穴（水晶障碍，视野受限）
- 地图选择界面，每张地图有独特敌人组合和 Boss
- 地图间解锁关系

### 9. 成就/统计系统
**现状**：只有最高分、总击杀、游戏次数三个全局统计。

**需要做**：
- 10-20 个成就（首次通关、单局击杀100、不受伤通关、收集所有武器等）
- 详细统计面板（每局数据、武器使用率、死亡原因分布）
- 成就解锁弹窗

---

## 四、P2 — 体验打磨（展示完成度）

### 10. 游戏内设置
**现状**：主菜单有"设置"按钮，功能可能未实现或很简陋。

**需要做**：
- 音量控制（BGM/SFX 分开）
- 画质切换（粒子开关、屏幕震动开关）
- 按键说明（PC 端 WASD + 鼠标，移动端摇杆）
- 暂停菜单内也能访问设置

### 11. 难度曲线与平衡性
**现状**：平衡还在迭代中，后期 Boss 强度不足，经济曲线前紧后松。

**需要做**：
- 完整的难度曲线设计（前10波教学、10-20波压力上升、20波后质变）
- 敌人属性随波次的平滑增长公式
- 武器数值全面平衡（目前激光过强，部分武器弱势）
- 商店经济曲线调整

### 12. 性能优化
**现状**：敌人数量多时可能掉帧（物理碰撞 + 渲染）。

**需要做**：
- 敌人对象池已实现，确认无泄漏
- 离屏敌人的更新节流（远处敌人降低 AI 频率）
- 粒子数量上限
- 物理碰撞的空间分区优化（目前是全量 collider）
- 低端设备的画质降级选项

### 13. 移动端体验
**现状**：有虚拟摇杆，但 UI 是按桌面端布局的。

**需要做**：
- 移动端 UI 自适应（按钮更大、HUD 重新布局）
- 触控热区优化
- 防误触（暂停按钮位置）
- 横竖屏锁定（建议横屏）

### 14. 新手引导完善
**现状**：有分步引导，但可能不够直观。

**需要做**：
- 首次游戏的完整教学（移动、攻击、升级、商店、Boss）
- 可跳过选项
- 关键机制的高亮提示（如第一次遇到 Boss）

---

## 五、P3 — 元游戏与重玩价值（可选加分项）

### 15. 永久升级（Roguelite 元素）
- 用游戏内货币解锁永久属性加成（初始血量+、初始金币+、经验加成+等）
- 武器/角色解锁树
- 每局结束后获得"灵魂"或"代币"用于永久升级

### 16. 每日挑战/种子模式
- 固定种子的每日挑战，全球排行榜（如果有后端）
- 自定义种子模式

### 17. 武器进化/超武
- 特定武器 + 特定被动达到满级后进化为超级武器（参考 Vampire Survivors）
- 进化条件和特效

---

## 六、发布准备

### 18. 展示物料
- 游戏图标（512x512，多尺寸）
- 宣传截图（3-5张，展示战斗、升级、Boss、商店）
- 短 GIF/视频演示（15-30秒核心循环）
- 游戏简介文案（一句话卖点 + 详细说明）

### 19. 部署
- 构建产物部署到可公开访问的 URL（Vercel / Netlify / GitHub Pages）
- 自定义域名（可选）
- SEO meta 标签（og:image、description）

### 20. 代码工程
- 关键系统的注释和文档（架构图、数据驱动设计说明）
- README 完善（运行方式、技术栈、特色功能）
- 移除调试代码（__game 钩子、console.log）
- 构建产物清理（source map 可选）

---

## 七、建议的执行顺序

```
第1阶段（视觉冲击）：P0-1 美术风格定调 + 核心角色/敌人替换 → P0-3 音频 → P0-2 特效
第2阶段（UI 包装）：P0-4 主菜单/设置/升级/商店美化
第3阶段（内容填充）：P1-5 敌人扩充 → P1-6 Boss 多样化 → P1-8 多地图
第4阶段（系统深度）：P1-7 角色选择 → P1-9 成就系统
第5阶段（打磨）：P2 全部 → P3 可选 → 发布准备
```

**最小可展示版本（MVP+）**：完成 P0 全部 + P1-5/6（敌人和 Boss 扩充），即可作为有视觉完成度的作品展示。

---

## 八、当前技术债务备注

- `src/main.ts` 的 `__game` 调试钩子需在发布前移除
- 部分场景用 `as any` 访问其他场景的方法，建议定义场景接口
- 武器平衡数据散落在各 Weapon 子类，建议集中到 `data/weapons.ts` 统一管理
- 地形障碍物配置已数据驱动（terrain.ts 每关独立配置），但颜色/尺寸等仍需按主题微调
- AudioManager 已就绪但 PreloadScene 未加载任何音频资源

> **2026-09-11 复盘新增（待用户确认后实施，勿擅自动手）**：项目 15 天 190 提交复盘，识别 5 项工程改进，均未开始：
> 1. **大文件拆分**：TextureGenerator(1739) / GameScene(1362) / Enemy(1178) / Player(1043) 四个文件超千行——GameScene 按波次调度/道具生成/升级流程/输入拆模块；Enemy 拆行为与词缀（词缀未来扩展）；TextureGenerator 拆纹理定义数据与生成逻辑；Player 拆属性计算与表现层
> 2. **移动端布局系统化**：大量 UI 用像素绝对坐标导致频繁错位（摇杆偏移/血条跑位/成就页空白）；将 DebugPanel 已验证的锚点+相对偏移布局模式推广到全部 UI，替代写死坐标
> 3. **回归测试**："改 A 坏 B" 频发（feat:fix ≈ 1.25:1）；基于试玩场地加脚本化断言（刷怪→施放→校验伤害/击杀数），提交前跑一遍
> 4. **提交规范统一**：190 提交中 74 个自由中文标题与 conventional 混用；后续统一 feat/fix/style/docs/refactor/test 前缀
> 5. **文档同步**：PROJECT_NOTES 与提交日志同步需更勤

---

## 九、移动设备发热风险分析

> **2026-09-15 新增**：对游戏主循环、渲染管线、物理碰撞、实体更新、特效系统逐层代码审查后，确认存在多个足以导致移动设备异常发热的性能瓶颈。

### 严重级 — 直接导致发热

#### 9.1 渲染分辨率对移动端过高

`main.ts` 第 40-47 行的 `computeRenderScale()`：

```typescript
const dpr = Math.min(window.devicePixelRatio || 1, 2);
const fit = Math.min(window.innerWidth / 960, window.innerHeight / 640);
return Math.min(Math.max(fit, 1) * dpr, cap);
```

移动端 dpr 通常为 2-3，fit 约 1.0-1.5，所以 `renderScale` 会被拉到 2-3，再按画质封顶。最终内部分辨率：

| 画质 | 封顶 | 内部分辨率 | 每帧像素数 | 60fps 像素吞吐 |
|---|---|---|---|---|
| low | 1.5 | 1440×960 | 1.38M | 83M/s |
| medium | 2.0 | 1920×1280 | 2.46M | **147M/s** |
| high | 2.5 | 2400×1600 | 3.84M | **230M/s** |

移动端 GPU 的填充率上限通常在 200-500M 像素/秒，但这是**理论峰值**——实际还要同时做混合、纹理采样、Canvas2D bridge。medium 画质下每秒 147M 像素已经接近移动 GPU 的舒适区上限，high 画质下 230M 几乎肯定会导致 GPU 满载发热。

#### 9.2 画质检测不考虑移动端

`GameManager.detectQuality()` 第 119-126 行：

```typescript
private detectQuality(): QualityLevel {
  const memory = (navigator as any).deviceMemory || 4;
  const cores = navigator.hardwareConcurrency || 4;
  if (memory <= 2 || cores <= 2) return 'low';
  if (memory >= 8 && cores >= 8) return 'high';  // 旗舰手机命中此条件
  return 'medium';
}
```

旗舰手机普遍报告 8GB+ RAM 和 8 核 CPU，因此被检测为 **high** 画质——200 个同屏敌人、resolutionScale 2.5、enableShadows true、enablePostFX true。`isMobile` 已被检测但**完全没有参与画质分级**，导致旗舰手机被当作桌面高性能设备对待。

#### 9.3 粒子发射器频繁创建/销毁

`FXManager.emit()` 第 61-73 行：

```typescript
const emitter = this.scene.add.particles(x, y, texture, { ... });
this.scene.time.delayedCall(lifespan + 120, () => emitter.destroy());
```

每次命中/死亡/拾取都创建一个**新的 ParticleEmitter 对象**，用完后销毁。在密集战斗中（200 敌人、多武器同时开火），每秒可能创建数十个发射器。这种 create/destroy churn 导致：
- 频繁 GC 压力（emitter 是重量级对象，含粒子池、shader 配置）
- WebGL 状态频繁切换（每次创建新 emitter 可能触发 shader/texture binding 变更）

相比之下，`DamageTextManager` 已正确池化 Text 对象，但粒子和近战挥砍 Graphics 没有池化。

### 中等级 — 持续推高负载

#### 9.4 每帧多次全数组遍历（O(n) 多趟）

一个帧内对 enemies 数组的遍历次数（medium 画质 120 敌人时）：

| 调用点 | 遍历对象 | 每帧次数 |
|---|---|---|
| `GameScene.update` — enemies.children.each | 敌人 | 1 |
| `GameScene.update` — bullets.children.each | 子弹 | 1 |
| `GameScene.update` — pickups.children.each | 拾取物 | 1 |
| `Enemy.update` → `avoidObstacles` | 障碍物 | ×n 敌人 |
| `Player.fireNova` / `fireMelee` / `findNearestEnemy` | 敌人 | 每次开火 1 |
| `HealerAI`（每 1.2s） | 敌人 | ×healer 数 |
| `Drone.update` | 敌人+子弹 | ×drone 数 |
| `Minimap.update` | 敌人+障碍物 | 1 |
| `HUD.update` | — | 1（多 setText） |

核心问题是 **`avoidObstacles`**：每个敌人每帧遍历全部障碍物（O(m)），总复杂度 O(n×m)。medium 画质下 120 敌人 × ~15 障碍物 = 1800 次/帧 × 60fps = 108,000 次/秒。high 画质下 200×20=4000 次/帧 = 240,000 次/秒。这个量级 CPU 不会过载，但是**它发生在每个敌人的 update 中，与物理引擎的 body 同步、setVelocity 串行执行，无法被 Phaser 批量化**。

#### 9.5 HUD 每帧 setText（触发纹理重栅格化）

`HUD.update()` 第 309-336 行每帧调用 5 次 `setText()`：

```typescript
this.waveText.setText(`波次: ${runData.wave}...`);
this.killsText.setText(`击杀: ${runData.kills}`);
this.scoreText.setText(`分数: ${runData.score}`);
this.timeText.setText(this.formatTime(runData.survivalTime));
this.coinText.setText(`💰 ${player.getCoins?.() ?? 0}`);
```

Phaser 的 Text 不是矢量字体——每次 `setText` 若文本变化，内部会**重新光栅化到 Canvas 纹理**。波次/击杀/分数/时间/金币每帧都可能变化（尤其时间是持续递增的），每帧 5 次纹理重生成是显著的 CPU+GPU 开销。

#### 9.6 小地图每帧 Graphics.clear() + 全量重绘

`Minimap.update()` 每帧 `graphics.clear()` 然后遍历所有敌人 + 障碍物重绘。120 敌人时 = 120 次 `fillRect` + 障碍物绘制，每帧重建 Graphics 命令缓冲。Graphics 不是 retained mode，每帧 clear + redraw 在移动端会产生持续 Canvas2D 负载。

### 较低级 — 辅助贡献

#### 9.7 近战挥砍 Graphics 对象创建

`Player.createMeleeSlash()` 第 562 行每次近战攻击创建新 Graphics 对象，150ms 后销毁。高攻速下（attackSpeed 2+）每秒 2+ 次 create/destroy。

#### 9.8 DamageText Tween 创建

虽然 DamageTextManager 池化了 Text 对象，但每次 `show()` 仍创建新的 tween。nova 武器一次命中 50 个敌人 = 瞬间 50 个 tween 对象创建。

### 结论

**存在足以导致移动设备异常发热的性能瓶颈**，核心原因有三个：

1. **GPU 填充率超标**：renderScale 2-2.5x 导致 medium/high 画质下每帧 2.5-3.8M 像素，移动端 GPU 长期满载
2. **画质检测缺陷**：旗舰手机被误判为 high 画质（200 敌人 + shadows + postFX），`isMobile` 未参与降级
3. **粒子发射器 create/destroy churn**：密集战斗中每秒数十次 emitter 创建销毁，GC 压力持续

这三者叠加后，移动端在战斗激烈场景下 GPU 满载 + CPU 高频 GC + 物理 O(n×m) 遍历，持续 60fps 拉满，**足以在数分钟内导致设备明显发热**。

### 建议修复方向

1. **`detectQuality` 加入 `isMobile` 判断**：移动端强制不超过 medium 画质，旗舰手机也走 medium（maxEnemies 120, resolutionScale 2, no postFX）
2. **移动端 renderScale 封顶 1.5**：在 `computeRenderScale` 中若 `isMobile` 则 `cap = min(cap, 1.5)`，将像素吞吐降到 83M/s
3. **粒子发射器池化**：FXManager 预创建 3-5 个 emitter 轮转复用，而非每次 new + destroy
4. **HUD setText 节流**：波次/击杀/分数仅在值变化时 setText，时间文本降频到每 500ms 更新一次
5. **小地图降频**：移动端每 2-3 帧更新一次小地图（肉眼不可察觉，减半 Canvas2D 负载）
6. **近战挥砍 Graphics 池化**：预创建 2-3 个 Graphics 对象轮转复用

---

## 十、其他建议
- 区域特色还不够明显，废墟可以设计一些陷阱，比如掉落陷阱的石头，玩家需要小心。区域内目前仅有障碍物，没有其他机制。（待定：区域机制模块化设计）