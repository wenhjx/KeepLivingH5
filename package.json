{
  "name": "keep-living-h5",
  "version": "0.1.0",
  "description": "Keep Living - A 2D survival roguelike game built with Phaser 3 + TypeScript + Vite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "audit": "node node_modules/esbuild/bin/esbuild scripts/balance-audit.ts --bundle --platform=node --format=cjs --outfile=scripts/.audit.cjs && node scripts/.audit.cjs && del scripts\\.audit.cjs"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
